---
phase: 04-csv-export-console-summary
plan: 2
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/main.f90
  - results.csv
  - .gitignore
autonomous: true
requirements: [OUT-01, OUT-02]
must_haves:
  truths:
    - "A single `fpm run` runs the full pipeline load → physics → diurnal → scenarios → results.csv + console summary with no manual intermediate steps (D-09, criterion 4)"
    - "results.csv in the project root has exactly 168 data rows + 1 header (14 occupied × 3 scenarios × 4 timesteps) in order scenario→timestep→i→j (D-01, D-02)"
    - "Console prints a baseline per-timestep table (4 rows: hottest name+feels, coolest name+feels, city-avg feels, urban-rural gap) + a scenario-delta recap of city-avg ΔT for add_trees and more_concrete (D-06, D-07)"
    - "uhi_offset_c values written to the CSV are the APPLIED offset m(t)·uhi_offset collected from the scenario-mutated work grid (so add_trees/more_concrete differ) (D-04)"
    - "[must-NOT] t_air is reference-only — feels_like_c is still called with base_t (diurnal_base), never with t_air (D-03 / WR-01)"
    - "[must-NOT] the console never prints per-cell t_air next to feels (D-08)"
    - "[must-NOT] no Phase 2-3 kernel file is modified (feels/uhi/diurnal/scenario/heat_index) — output-only phase"
  artifacts:
    - path: "app/main.f90"
      provides: "driver that collects feels_all+uhi_all, calls write_results_csv, and prints the OUT-02 baseline table + scenario-delta recap"
      contains: "write_results_csv"
    - path: "results.csv"
      provides: "deterministic CSV deliverable (169 lines) regenerated each run"
  key_links:
    - from: "app/main.f90"
      to: "src/io.f90 write_results_csv"
      via: "single call after the scenario/timestep loops with collected feels_all + uhi_all"
      pattern: "call write_results_csv"
    - from: "app/main.f90"
      to: "src/summary.f90 hottest/coolest/city_average/urban_rural_gap"
      via: "per-timestep reductions over baseline feels for the OUT-02 table"
      pattern: "call hottest|call coolest"
    - from: "app/main.f90"
      to: "src/uhi.f90 uhi_offset"
      via: "uhi_offset_c = m_t * uhi_offset(...) collected per scenario×timestep×cell"
      pattern: "uhi_offset\\("
    - from: "app/main.f90 feels computation"
      to: "src/diurnal.f90 diurnal_base"
      via: "feels_like_c(base_t, m_t, ...) — base_t from diurnal_base, NOT t_air"
      pattern: "feels_like_c\\(base_t"
---

<objective>
Wire the Wave-1 capabilities into the driver to complete the end-to-end deliverable (Wave 2):

1. Inside the EXISTING `scenario → timestep → (i,j)` loop in `app/main.f90`, retain feels into a
   4D array `feels_all(nx,ny,NT,3)` and collect the applied UHI offset into `uhi_all(nx,ny,NT,3)`
   from the scenario-mutated `work` grid (D-04, D-09).
2. After the loops, make ONE call to `write_results_csv('results.csv', baseline_grid, coeffs,
   feels_all, uhi_all, scen_labels, stat, msg)` and error-stop on failure (OUT-01; D-05, D-09).
3. Replace the minimal Phase-3 console lines with the OUT-02 report: a baseline per-timestep table
   (hottest/coolest name+feels, city-avg feels, urban-rural gap) plus a scenario-delta recap
   (city-avg ΔT for add_trees / more_concrete), reusing `hottest`/`coolest`/`city_average`/
   `urban_rural_gap` — feels-like ONLY, never t_air next to feels (D-06, D-07, D-08).

Purpose: deliver criterion 4 — one `fpm run` produces a clean, plottable `results.csv` plus a
readable terminal summary. This is an OUTPUT-ONLY phase: feels is still computed with `base_t`
(NOT `t_air`), and no kernel is modified.

Output: rewired `app/main.f90`; generated `results.csv` (169 lines); `.gitignore` entry for the
generated artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-csv-export-console-summary/04-CONTEXT.md
@.planning/phases/04-csv-export-console-summary/04-PATTERNS.md
@.planning/phases/04-csv-export-console-summary/04-01-PLAN.md
@.planning/research/PITFALLS.md
@app/main.f90
@src/io.f90
@src/summary.f90
@src/diurnal.f90
@src/uhi.f90
@src/grid.f90
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire app/main.f90 — collect feels_all+uhi_all, call write_results_csv, print OUT-02 report</name>
  <files>app/main.f90</files>
  <read_first>
    - app/main.f90 (whole file) — the existing `use` block (lines 1-10), the local declarations
      (lines 12-26), the error-stop idiom (lines 28-32), the grid-layout print (lines 43-58), the
      scenario setup (lines 64-75), the scenario→timestep→(i,j) loop computing `feels_val` /
      `feels_current` / `feels_baseline` (lines 78-96), the `delta`/`avg_delta` computation (lines
      98-100), and the minimal console prints to REPLACE (lines 102-110).
    - src/io.f90 — the `write_results_csv` signature added in 04-01 (and its `stat`/`msg` contract).
    - src/summary.f90 — `hottest`/`coolest` (subroutine out-args ih/jh/val) plus existing
      `city_average`/`urban_rural_gap` (functions).
    - src/uhi.f90 — `uhi_offset(building, tree, water_km, is_urban, w_build, w_urban, w_tree,
      w_water, d0)` argument order (for the uhi_offset_c collection).
    - src/diurnal.f90 — `diurnal_base`/`diurnal_m`/`time_label`/`NT`.
    - .planning/research/PITFALLS.md §A5 (initialize every cell — feed `-finit-real=snan`), §A9.
  </read_first>
  <action>
    Extend the driver `use` block: `use io_mod, only: read_coeffs_nml, read_grid_csv,
    write_results_csv`; `use summary_mod, only: urban_rural_gap, city_average, hottest, coolest`;
    `use uhi_mod, only: uhi_offset` (new — for the uhi_offset_c column). `diurnal_mod` and
    `iso_fortran_env` (error_unit, output_unit) are already imported.

    Add declarations: `real(wp), allocatable :: feels_all(:,:,:,:)`, `real(wp), allocatable ::
    uhi_all(:,:,:,:)`, `real(wp) :: avg_delta_all(NT,3)`, `character(len=32) :: scen_labels(3)`,
    plus reduction scratch `integer :: ih, jh, ic, jc` and `real(wp) :: vh, vc, avg_t`. Allocate
    `feels_all(baseline_grid%nx, baseline_grid%ny, NT, 3)` and `uhi_all(...)` once.

    Inside the existing scenario→timestep→(i,j) loop, KEEP the unconditional `do j; do i` over every
    cell (compute feels for all cells before any masking — A5, so `-finit-real=snan` stays clean).
    After `feels_current(i,j) = feels_val`, also set `feels_all(i,j,it,iscen) = feels_val` and
    `uhi_all(i,j,it,iscen) = m_t * uhi_offset(work%cells(i,j)%building, work%cells(i,j)%tree,
    work%cells(i,j)%water_km, work%cells(i,j)%is_urban, coeffs%w_build, coeffs%w_urban,
    coeffs%w_tree, coeffs%w_water, coeffs%d0)`. Because `work` is the scenario-mutated grid, the
    applied offset differs across baseline/add_trees/more_concrete (D-04). CRITICAL: leave the
    `feels_like_c(base_t, m_t, work%cells(i,j)%rh, ...)` call EXACTLY as-is — it takes `base_t`
    (from `diurnal_base`), never `t_air` (D-03/WR-01; do not change the kernel call). Store the
    existing `avg_delta` into `avg_delta_all(it, iscen)`. Set `scen_labels(1)='baseline'`,
    `scen_labels(2)='add_trees'`, `scen_labels(3)='more_concrete'` (matching `scens%label`).

    REMOVE the per-timestep gap line and per-scenario dT line (current lines 102-110). Keep the
    "Loaded N districts" line and the grid-layout marker print (it prints `#`/`*`/`.` markers, not
    temperatures, so it does not show t_air next to feels — D-08 safe).

    Add the OUT-02 console report AFTER the loops (D-06). First a baseline table:
    `write(output_unit,'(A)') '--- Baseline summary (feels-like, C) ---'`, then an aligned column
    header, then `do it = 1, NT`: call `hottest(feels_baseline(:,:,it), baseline_grid, ih, jh, vh)`,
    `coolest(feels_baseline(:,:,it), baseline_grid, ic, jc, vc)`,
    `avg_t = city_average(feels_baseline(:,:,it), baseline_grid)`,
    `gap_t = urban_rural_gap(feels_baseline(:,:,it), baseline_grid)`, and write one aligned row:
    `trim(time_label(it))`, hottest `trim(baseline_grid%cells(ih,jh)%name)` + `vh`, coolest
    `trim(baseline_grid%cells(ic,jc)%name)` + `vc`, `avg_t`, `gap_t`. Use a fixed-width aligned
    format with generous widths (e.g. `'(A10,2X,A20,F7.2,2X,A20,F7.2,2X,F8.2,2X,F7.2)'`) — widths
    are Claude's-Discretion but must be wide enough to avoid field overflow (A9; district names are
    ≤ ~19 chars, feels ~24-40, gap single digits). Report feels-like ONLY — do not print t_air in
    this table (D-08). Then a recap: `write(output_unit,'(A)') '--- Scenario city-average dT (C) ---'`
    and for `iscen = 2, 3` print a line per scenario with its 4 `avg_delta_all(it,iscen)` values
    labelled by `trim(scen_labels(iscen))` and `trim(time_label(it))`.

    Finally, after the report, add the single CSV call: `call write_results_csv('results.csv',
    baseline_grid, coeffs, feels_all, uhi_all, scen_labels, stat, msg)` then the verbatim error-stop
    idiom `if (stat /= 0) then; write(error_unit,'(A)') trim(msg); error stop 1; end if`. The path
    is the hardcoded literal `'results.csv'` (D-05 — a configurable namelist `output_path` knob is
    deferred; do NOT add one). Every real literal carries `_wp`.
  </action>
  <acceptance_criteria>
    - `app/main.f90` `use` block adds `write_results_csv`, `hottest`, `coolest`, and `uhi_offset`.
    - `feels_all` and `uhi_all` are 4D `(nx,ny,NT,3)`, filled for EVERY cell inside the existing loop;
      `uhi_all` is built from `work` (scenario-mutated) so the three scenarios differ.
    - The `feels_like_c(...)` call still passes `base_t` (from `diurnal_base`) and NOT `t_air`
      (grep: `feels_like_c(base_t` present; no `feels_like_c(.*t_air` / no `feels_like_c(work%cells`
      passing t_air as the first temperature arg).
    - Exactly one `call write_results_csv('results.csv', ...)` exists, followed by the `error stop 1`
      failure guard.
    - The console prints `--- Baseline summary (feels-like, C) ---` with 4 timestep rows (hottest
      name+feels, coolest name+feels, city-avg, gap) and `--- Scenario city-average dT (C) ---` with
      add_trees and more_concrete recap lines; no per-cell t_air is printed next to feels (D-08).
    - `fpm build --flag "-std=f2018 -fcheck=all -ffpe-trap=invalid,zero,overflow -Wall -Wextra -finit-real=snan"` is clean (no warnings-as-errors surprises; unused-var clean).
    - `git diff --name-only` shows changes only to app/main.f90 (plus .gitignore in task 2) — NO
      kernel file (feels/uhi/diurnal/scenario/heat_index) modified.
  </acceptance_criteria>
  <verify>
    <automated>fpm build --flag "-std=f2018 -fcheck=all -ffpe-trap=invalid,zero,overflow -Wall -Wextra -finit-real=snan" && grep -q "feels_like_c(base_t" app/main.f90 && grep -q "call write_results_csv('results.csv'" app/main.f90</automated>
  </verify>
  <done>
    Driver collects feels_all + scenario-dependent uhi_all, calls write_results_csv once with
    error-stop, and prints the OUT-02 baseline table + scenario-delta recap (feels-like only);
    feels is still computed from base_t; build clean under strict flags; no kernel changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end pipeline run + results.csv artifact verification + .gitignore</name>
  <files>results.csv, .gitignore</files>
  <read_first>
    - app/main.f90 (the rewired driver from task 1).
    - data/hcmc_districts.csv — confirm 14 occupied district rows (so 14×3×4 = 168 data rows).
    - .planning/phases/04-csv-export-console-summary/04-CONTEXT.md — D-01 (168+1), D-02 (order),
      D-09 (single fpm run), D-05 (fixed results.csv overwrite).
  </read_first>
  <action>
    Run the full pipeline with a single `fpm run` and confirm it produces both deliverables with no
    manual intermediate steps (D-09, criterion 4). Verify `results.csv` in the project root: the
    first line is exactly the 9-column header `i,j,name,time_label,scenario,t_air,base_t,feels_c,
    uhi_offset_c`; the file has exactly 169 physical lines (168 data + 1 header, D-01); the data is
    locale-clean (decimal `.`, no field-overflow asterisks — guaranteed by the F0.2 descriptors from
    04-01); and the first data row is `baseline,morning` for the smallest occupied `(i,j)`
    (deterministic order, D-02). Confirm the console emitted the OUT-02 baseline table + scenario
    recap. Then add a `.gitignore` entry for `results.csv` (it is a regenerated build artifact,
    overwritten each run — keep it out of version control). Do NOT hand-edit `results.csv`; it is
    generated.
  </action>
  <acceptance_criteria>
    - `fpm run` exits 0 and creates `results.csv` in the project root.
    - `head -1 results.csv` equals exactly `i,j,name,time_label,scenario,t_air,base_t,feels_c,uhi_offset_c`.
    - `wc -l < results.csv` equals `169`.
    - The 2nd line (first data row) ends with the scenario field `baseline` and contains the
      time_label `morning`.
    - `.gitignore` contains a `results.csv` line.
    - Full regression: `fpm test --flag "-std=f2018 -fcheck=all -ffpe-trap=invalid,zero,overflow -Wall -Wextra -finit-real=snan"` exits 0 (all suites including 04-01's output_tests/summary_tests and all Phase 1-3 suites).
  </acceptance_criteria>
  <verify>
    <automated>fpm run && test "$(wc -l < results.csv)" -eq 169 && head -1 results.csv | grep -qx 'i,j,name,time_label,scenario,t_air,base_t,feels_c,uhi_offset_c' && sed -n '2p' results.csv | grep -q ',baseline,' && sed -n '2p' results.csv | grep -q ',morning,'</automated>
  </verify>
  <done>
    A single `fpm run` produces results.csv (169 lines, exact header, deterministic
    scenario→timestep→i→j order) plus the OUT-02 console report; full strict-flag test suite green;
    results.csv is gitignored.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-02)

New symbols/files created or first-materialized by this plan (the convergence source-grounding pass
must EXCLUDE these from drift verification — they are net-new in this phase):

- `app/main.f90` driver locals (not public symbols): `feels_all(nx,ny,NT,3)`, `uhi_all(nx,ny,NT,3)`,
  `avg_delta_all(NT,3)`, `scen_labels(3)`, and reduction scratch `ih,jh,ic,jc,vh,vc,avg_t`.
- New `use` edges in `app/main.f90`: `write_results_csv` (io_mod), `hottest`/`coolest`
  (summary_mod), `uhi_offset` (uhi_mod).
- `results.csv` — generated CSV deliverable in the project root (169 lines), overwritten each run
  (D-05). Added to `.gitignore`.
- `.gitignore` — new `results.csv` ignore rule.

Consumes (from 04-01, do not re-verify as drift): `write_results_csv`, `hottest`, `coolest`.
Consumes (locked Phases 2-3, MUST NOT modify): `feels_like_c`, `uhi_offset`, `diurnal_base`,
`diurnal_m`, `time_label`, `apply_scenario`, `city_average`, `urban_rural_gap`.

NOT created (explicitly deferred): no configurable `output_path` namelist knob (D-05 fixes
`results.csv`); no timestamped history file; no full-grid raster rows.
</artifacts_produced>

<threat_model>
## Trust Boundaries

Self-contained local Fortran batch program. Input is the pre-validated local data file (Phase 1).
This plan wires output into the driver and writes a fixed `results.csv`. No network, no auth, no
remote/untrusted input.

| Boundary | Description |
|----------|-------------|
| local data file → in-memory grid | already validated at load (Phase 1); unchanged here |
| in-memory feels/uhi arrays → `results.csv` | program data crosses into a written CSV file |
| program → fixed `results.csv` path | output path is a hardcoded literal, overwritten each run (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | output file path | accept | The path is the hardcoded literal `'results.csv'` — NOT derived from user/runtime input, so there is no path-traversal vector. Overwrite-each-run is the intended behavior (D-05). Informational. |
| T-04-06 | Information disclosure (wrong science) | feels computation in the driver loop | mitigate | Feels is computed with `base_t` (from `diurnal_base`), never `t_air` — `t_air` is written as a reference-only CSV column and never fed to `feels_like_c` (D-03/WR-01), avoiding the double-counted-UHI error (PITFALLS B3). The `feels_like_c` kernel call is left unchanged; grep-verified. Low. |
| T-04-07 | Denial of service | uninitialized array reads under reductions/CSV write | mitigate | feels_all and uhi_all are filled for EVERY cell inside the unconditional `do j; do i` loop before any masked reduction or CSV write (PITFALLS A5), so `-finit-real=snan` finds no uninitialized read. Low. |
| T-04-02 | Data integrity | numeric CSV fields | mitigate | Inherited from 04-01: width-free `F0.2` descriptors → `.` decimals (A9), no field-overflow asterisk fill (A9). End-to-end run asserts header + 169 lines. Low. |
| T-04-SC | Tampering (supply chain) | npm/pip/cargo/fpm installs | accept | No new dependencies; `test-drive` pinned at tag `v0.6.0` (existing). No install step. |

No high-severity threats: no network, no auth, no remote/untrusted input, no runtime-derived path.
</threat_model>

<verification>
- `fpm build --flag "-std=f2018 -fcheck=all -ffpe-trap=invalid,zero,overflow -Wall -Wextra -finit-real=snan"` clean.
- `fpm run` exits 0, prints the OUT-02 baseline table + scenario recap, and writes `results.csv`.
- `results.csv` = 169 lines; first line == exact 9-column header; first data row is `baseline`/`morning`.
- `fpm test --flag "-std=f2018 -fcheck=all -ffpe-trap=invalid,zero,overflow -Wall -Wextra -finit-real=snan"` exits 0 (04-01 suites + all Phase 1-3 suites; regression intact).
- `git diff --name-only` shows only app/main.f90 + .gitignore changed by this plan — no kernel file touched.
</verification>

<success_criteria>
- A single `fpm run` executes load → physics → diurnal → scenarios → CSV + console with no manual
  steps (criterion 4, D-09).
- `results.csv` has 168 data rows + 1 header (14×3×4) in scenario→timestep→i→j order, exact 9-column
  header, `.`-decimal asterisk-free numeric fields (criterion 1 & 2, OUT-01).
- Console prints the baseline per-timestep table (hottest/coolest name+feels, city-avg, gap) + the
  scenario-delta recap, feels-like only (criterion 3, OUT-02; D-06/D-07/D-08).
- t_air never feeds feels; no Phase 2-3 kernel modified.
</success_criteria>

<output>
Create `.planning/phases/04-csv-export-console-summary/04-02-SUMMARY.md` when done.
</output>